import { GrantsPageClient } from "@/components/grants/grants-page-client";

export const metadata = {
  title: "Grants & Innovation Engine | National Land Governance Platform",
  description:
    "Action layer transforming contradiction engine findings and dispute intelligence gaps into fundable, trackable research projects.",
};

export default function GrantsPage() {
  return <GrantsPageClient />;
}
